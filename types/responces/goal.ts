export type DtoSingleGoal = {
  activityLevel: string;
  id: string;
  userId: string;
  carbsTarget: number;
  fatTarget: number;
  kcalTarget: number;
  proteinTarget: number;
  weightCurrent: number;
  weightTarget: number;
  createdAt: string;
  updatedAt: string;
  deadline: string;
}